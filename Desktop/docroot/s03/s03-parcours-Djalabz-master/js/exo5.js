/* === Exo5 ===
 *
 * Dans la fonction ci-dessous, le paramètre int est un entier.
 * Le doubler, et le retourner.
 */

function doubleInt(int) {

    var double = Number(int * 2);
    return double;

}






// Stop ! Tests, on ne touche pas :P

check.exo5(doubleInt);